package com.spendwise.app.core.common

object AppConstants {
    /** Temporary local identity used until Firebase Authentication is wired. */
    const val DEMO_USER_ID = "local-demo-user"
    const val DEFAULT_CURRENCY = "VND"
}

